let value;

value = window;

value = window.document;

// Sayfadaki bütün elemanları getirir (html, head, meta, title, body, h1).
//value = document.all;

// elemanlardan 6. ya eriştik.
// value = document.all[6];

let array = Array.from(value);

// array.forEach(function(item) {
//     console.log(item);
// });

value = document.boy;
value = document.head;
value = document.domain;
value = document.URL;
value = document.contentType;

//value = document.forms[0].id;
//value = document.links[1].className;
//value = document.links[1].classList;

//value = document.images[0].getAttribute('src');

value = document;


console.log(value);